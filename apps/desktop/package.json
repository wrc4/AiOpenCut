{
  "name": "aiopencut",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web:dev": "cd ../web && bun run dev",
    "tauri:dev": "tauri dev",
    "dev": "concurrently \"bun run web:dev\" \"bun run tauri:dev\"",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "@tauri-apps/cli": "^2.0.0"
  }
}
